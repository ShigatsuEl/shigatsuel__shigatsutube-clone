mixin videoBlock(video ={})
    .videoBlock
        a(href=routes.videoDetail(video.id))
            video.videoBlock__thumbnail(src=video.videoFile)
            .videoBlock__description
                .left
                    img.videoBlock__img(src=video.imgFile)
                .right
                    h6.videoBlock__title=video.title
                    h6.videoBlock__creator=`Uploaded by ${video.name}`
                    h6.videoBlock__views=`VIEWS ${video.views} · ${dateFormatter(video.createdAt)}`